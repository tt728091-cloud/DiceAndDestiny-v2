# Features

Owns prompt/story-sized client feature slices.

Feature folders should stay narrow and may coordinate local client code, presentation components, fixtures, and tests for one gameplay capability.

Shared presentation widgets belong under `presentation/`. Shared authority adapters belong under `local_client/`.
