# Snapshot

Reserved for Godot-local combat snapshots only if a Godot authority path is chosen.

For the current Go authority path, snapshots are returned by Go and transformed into view state by Godot.
