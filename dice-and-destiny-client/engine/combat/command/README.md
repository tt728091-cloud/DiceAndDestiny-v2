# Command

Reserved for Godot-local combat commands only if a Godot authority path is chosen.

For the current Go authority path, command construction belongs in `local_client/command_builder/` and command validation belongs in Go.
