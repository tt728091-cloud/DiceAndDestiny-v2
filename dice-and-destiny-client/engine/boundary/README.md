# Boundary

Reserved for a local Godot battle-engine boundary if we ever implement a Godot authority path.

For the current Go authority path, use `local_client/battle_gateway/` and the Go authority instead.
